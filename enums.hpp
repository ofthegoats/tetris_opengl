#ifndef ENUMS_H_
#define ENUMS_H_

enum Rotation { CounterClockwise, Clockwise };

enum Square {  // standard tetromino colours and empty square
    Empty,
    Cyan,
    Blue,
    Orange,
    Yellow,
    Green,
    Pink,
    Red
};

enum Piece { I, J, L, O, S, T, Z };

#endif  // ENUMS_H_
